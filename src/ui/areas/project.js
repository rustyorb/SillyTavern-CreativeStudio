import { html, Empty } from '../kit.js';
import { uid } from '../../core/bytes.js';
export function ProjectArea() {
    return html`<${Empty} icon="hammer" title="project">Under construction.</${Empty}>`;
}
