import { html, Empty } from '../kit.js';
import { uid } from '../../core/bytes.js';
export function CharactersArea() {
    return html`<${Empty} icon="hammer" title="characters">Under construction.</${Empty}>`;
}
