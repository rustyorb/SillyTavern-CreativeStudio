import { html, Empty } from '../kit.js';
import { uid } from '../../core/bytes.js';
export function PlaytestArea() {
    return html`<${Empty} icon="hammer" title="playtest">Under construction.</${Empty}>`;
}
