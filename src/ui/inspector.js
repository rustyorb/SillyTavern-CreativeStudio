import { html, Empty } from './kit.js';
export function Inspector() {
    return html`<aside class="cs-inspector"><${Empty} icon="wand-magic-sparkles" title="Inspector">Proposals, history and diagnostics.</${Empty}></aside>`;
}
